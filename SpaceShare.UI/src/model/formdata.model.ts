export interface FormData {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  birthdate: Date;
  region: string;
  province: string;
  city: string;
  postalCode: string;
}
